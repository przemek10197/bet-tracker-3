export enum BetResult {
  UNKNOWN = 'WAITING',
  WIN = 'WINNING',
  PUSH = 'PUSH',
  LOSE = 'LOST',
}

export enum LegResult {
  UNKNOWN = 'UNKNOWN',
  WIN = 'WINNING',
  PUSH = 'PUSH',
  LOSE = 'LOST',
}
